import { GalleryClient } from "@/components/gallery/GalleryClient";

export const metadata = {
  title: "Product Gallery | Prince Pipes & Fittings",
  description: "Explore our premium product gallery featuring a wide range of precision-engineered industrial pipe fittings, flanges, and piping components.",
};

export default function GalleryPage() {
  return <GalleryClient />;
}
